spring:
  application:
    name: Lovable

  datasource:
    url: jdbc:postgresql://localhost:9010/pgvector-test
    username: user #postgres
    password: password #root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  ai:
    openai:
      api-key:
      base-url: https://openrouter.ai/api
      chat:
        options:
          model: openai/gpt-5.2-codex
          temperature: 0.0  # Adjust as needed for creativity (0.0 = deterministic, higher values = more creative)
jwt:
  secret-key: lkasfdiha98wefyaewlfnadsf9wkjl wnfda9wut4wns0jqw3flkjnasd3r2wesff


stripe:
  api:
    secret: ${STRIPE_SECRET_KEY}
  webhook:
    secret: whsec_091ae3fdab8294f6aa1d324cbea02306ff314e4042795755afe8ce423970ddef

client:
  url: http://localhost:8000

minio:
  url: http://localhost:9000          # or minio:9000 if inside docker network
  access-key: minioadmin
  secret-key: minioadmin123
  project-bucket: projects